---
phase: 05-browser-skills-qa-office-hours
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime/office-hours/mode.ts
  - src/runtime/office-hours/memo.ts
  - src/skills/office-hours/build-office-hours-skill.ts
  - .github/skills/office-hours/SKILL.md
  - .github/skills/office-hours/README.md
  - tests/fixtures/office-hours-skill.md
  - tests/runtime/office-hours-mode.test.ts
  - tests/runtime/office-hours-memo.test.ts
  - tests/skills/office-hours-artifact.test.ts
  - tests/integration/office-hours-runtime.test.ts
  - tests/integration/office-hours-convert.test.ts
autonomous: true
requirements:
  - SKILL-03
must_haves:
  truths:
    - "The checked-in `/office-hours` skill returns both conversational feedback and a durable memo artifact."
    - "The `/office-hours` memo is critique-first and ends with a concise recommended direction."
    - "The `/office-hours` port preserves the source skill's startup-vs-builder mode distinction at the contract level."
  artifacts:
    - path: "src/runtime/office-hours/mode.ts"
      provides: "Mode-selection contract for /office-hours"
      exports: ["determineOfficeHoursMode"]
    - path: "src/runtime/office-hours/memo.ts"
      provides: "Structured memo schema/template for /office-hours"
      exports: ["OfficeHoursMemo", "createOfficeHoursMemo"]
    - path: "src/skills/office-hours/build-office-hours-skill.ts"
      provides: "Builder for the checked-in /office-hours artifact"
      exports: ["buildOfficeHoursSkillArtifact", "buildOfficeHoursReadme"]
    - path: ".github/skills/office-hours/SKILL.md"
      provides: "Project-local /office-hours skill artifact"
  key_links:
    - from: "tests/runtime/office-hours-mode.test.ts"
      to: "src/runtime/office-hours/mode.ts"
      via: "import"
      pattern: "import.*determineOfficeHoursMode"
    - from: "tests/runtime/office-hours-memo.test.ts"
      to: "src/runtime/office-hours/memo.ts"
      via: "import"
      pattern: "import.*OfficeHoursMemo"
    - from: "tests/skills/office-hours-artifact.test.ts"
      to: "src/skills/office-hours/build-office-hours-skill.ts"
      via: "import"
      pattern: "import.*buildOfficeHoursSkillArtifact"
---

<objective>
Port `/office-hours` into a checked-in Copilot skill that stays grounded in browser observations while preserving the original source skill's core reasoning structure: startup/builder modes and a durable design memo.

Purpose: make `/office-hours` a real browser-dependent Copilot skill without losing the source skill's strongest behavior, which is structured product thinking that ends in an actionable memo.

Output: `/office-hours` mode helper, memo schema, builder, checked-in artifact, README, and focused runtime/artifact/integration tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-browser-skills-qa-office-hours/05-CONTEXT.md
@.planning/phases/05-browser-skills-qa-office-hours/05-RESEARCH.md
@.planning/phases/04-browser-abstraction/04-VERIFICATION.md
@src/runtime/browser/adapter.ts
@src/runtime/browser/capabilities.ts
@src/runtime/browser/fallbacks.ts
@src/skills/review/build-review-skill.ts
@.github/skills/review/SKILL.md
@C:/Users/Thinkpad/.codex/skills/gstack-office-hours/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define `/office-hours` mode and memo contracts</name>
  <files>src/runtime/office-hours/mode.ts, src/runtime/office-hours/memo.ts, tests/runtime/office-hours-mode.test.ts, tests/runtime/office-hours-memo.test.ts</files>
  <read_first>
    - .planning/phases/05-browser-skills-qa-office-hours/05-CONTEXT.md
    - .planning/phases/05-browser-skills-qa-office-hours/05-RESEARCH.md
    - C:/Users/Thinkpad/.codex/skills/gstack-office-hours/SKILL.md
    - src/runtime/browser/adapter.ts
  </read_first>
  <acceptance_criteria>
    - src/runtime/office-hours/mode.ts exports `determineOfficeHoursMode`
    - `determineOfficeHoursMode` supports at least startup and builder modes
    - src/runtime/office-hours/memo.ts exports `OfficeHoursMemo` and `createOfficeHoursMemo`
    - `OfficeHoursMemo` includes critique sections plus a `recommendedDirection` field
    - `OfficeHoursMemo` includes an open-questions field and an actionable next-step/assignment field
    - tests/runtime/office-hours-mode.test.ts exits 0
    - tests/runtime/office-hours-memo.test.ts exits 0
  </acceptance_criteria>
  <action>
    Create `src/runtime/office-hours/mode.ts` to encode the source skill's startup-versus-builder distinction as a runtime contract the port can test.

    Create `src/runtime/office-hours/memo.ts` as the structured output schema for the durable memo artifact. The schema must support:
    - critique-first observations grounded in what the skill saw
    - approaches considered
    - concise recommended direction
    - open questions
    - a concrete next step or assignment

    Keep the schema broad enough to support both startup and builder postures without forcing full source-skill ceremony into every output.
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/office-hours-mode.test.ts tests/runtime/office-hours-memo.test.ts</automated>
  </verify>
  <done>The repository has a real `/office-hours` output contract instead of a prose-only skill description.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the checked-in `/office-hours` skill artifact</name>
  <files>src/skills/office-hours/build-office-hours-skill.ts, .github/skills/office-hours/SKILL.md, .github/skills/office-hours/README.md, tests/fixtures/office-hours-skill.md, tests/skills/office-hours-artifact.test.ts</files>
  <read_first>
    - src/skills/review/build-review-skill.ts
    - .github/skills/review/SKILL.md
    - src/runtime/office-hours/mode.ts
    - src/runtime/office-hours/memo.ts
    - .planning/phases/05-browser-skills-qa-office-hours/05-CONTEXT.md
    - C:/Users/Thinkpad/.codex/skills/gstack-office-hours/SKILL.md
  </read_first>
  <acceptance_criteria>
    - src/skills/office-hours/build-office-hours-skill.ts exports `buildOfficeHoursSkillArtifact` and `buildOfficeHoursReadme`
    - .github/skills/office-hours/SKILL.md exists and matches the builder output exactly
    - .github/skills/office-hours/SKILL.md describes both conversational feedback and memo output
    - .github/skills/office-hours/SKILL.md explicitly says the memo is critique-first and ends with a recommended direction
    - .github/skills/office-hours/SKILL.md does not start implementation or scaffold code
    - tests/skills/office-hours-artifact.test.ts exits 0
  </acceptance_criteria>
  <action>
    Create `tests/fixtures/office-hours-skill.md` as a hermetic source fixture for the source gstack `/office-hours` skill.

    Create `src/skills/office-hours/build-office-hours-skill.ts` following the `/review` builder pattern:
    - keep the builder as the source of truth for `.github/skills/office-hours/SKILL.md`
    - encode the locked Phase 5 decisions directly in artifact guidance
    - preserve the startup-versus-builder framing from the source skill where it affects output behavior

    Create `.github/skills/office-hours/SKILL.md` and `.github/skills/office-hours/README.md`.
    The artifact text must explicitly say:
    - the skill analyzes what it observes through the browser runtime
    - it returns conversational feedback plus a memo artifact
    - the memo is critique-first, then ends with a concise recommended direction
    - the skill does not begin implementation work
  </action>
  <verify>
    <automated>npx vitest run tests/skills/office-hours-artifact.test.ts</automated>
  </verify>
  <done>The repository has a checked-in `/office-hours` artifact aligned with the Phase 5 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add `/office-hours` builder and runtime integration coverage</name>
  <files>tests/integration/office-hours-runtime.test.ts, tests/integration/office-hours-convert.test.ts</files>
  <read_first>
    - src/runtime/office-hours/mode.ts
    - src/runtime/office-hours/memo.ts
    - src/skills/office-hours/build-office-hours-skill.ts
    - .github/skills/office-hours/SKILL.md
    - tests/fixtures/office-hours-skill.md
  </read_first>
  <acceptance_criteria>
    - tests/integration/office-hours-runtime.test.ts exists and exits 0
    - tests/integration/office-hours-convert.test.ts exists and exits 0
    - integration tests assert startup-versus-builder mode behavior remains encoded
    - integration tests assert the memo contract includes critique sections plus recommended direction
    - integration tests assert the checked-in artifact is parseable and preserves the do-not-implement rule
  </acceptance_criteria>
  <action>
    Add integration coverage that validates the runtime helpers and builder output together.

    `tests/integration/office-hours-runtime.test.ts` must cover:
    - mode selection behavior
    - memo schema creation
    - critique-first output shape with a required recommended direction

    `tests/integration/office-hours-convert.test.ts` must cover:
    - builder output is parseable
    - the checked-in artifact includes both conversation and memo guidance
    - the checked-in artifact keeps implementation explicitly out of scope
  </action>
  <verify>
    <automated>npx vitest run tests/integration/office-hours-runtime.test.ts tests/integration/office-hours-convert.test.ts</automated>
  </verify>
  <done>The `/office-hours` port is validated as a coherent runtime-plus-artifact slice.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/runtime/office-hours-mode.test.ts tests/runtime/office-hours-memo.test.ts tests/skills/office-hours-artifact.test.ts tests/integration/office-hours-runtime.test.ts tests/integration/office-hours-convert.test.ts`
2. `npx tsc --noEmit`
3. `.github/skills/office-hours/SKILL.md` reflects the locked Phase 5 `/office-hours` decisions and preserves the no-implementation rule
</verification>

<success_criteria>
- `/office-hours` mode contract exists
- Durable memo contract exists
- `/office-hours` checked-in artifact exists and is builder-backed
- Runtime, artifact, and integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-browser-skills-qa-office-hours/05-02-SUMMARY.md`
</output>
