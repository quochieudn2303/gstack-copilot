---
phase: 05-browser-skills-qa-office-hours
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime/qa/flow.ts
  - src/runtime/qa/report.ts
  - src/skills/qa/build-qa-skill.ts
  - .github/skills/qa/SKILL.md
  - .github/skills/qa/README.md
  - tests/fixtures/qa-skill.md
  - tests/runtime/qa-flow.test.ts
  - tests/runtime/qa-report.test.ts
  - tests/skills/qa-artifact.test.ts
  - tests/integration/qa-runtime.test.ts
  - tests/integration/qa-convert.test.ts
autonomous: true
requirements:
  - SKILL-02
must_haves:
  truths:
    - "The checked-in `/qa` skill defaults to a guided flow and reports findings before any edits."
    - "The `/qa` contract requires explicit confirmation before it may enter a same-session fix loop."
    - "The `/qa` report model captures screenshots, repro steps, severity, and console/network evidence when relevant."
  artifacts:
    - path: "src/runtime/qa/flow.ts"
      provides: "Guided-flow and fix-gating runtime contract for /qa"
      exports: ["resolveQaMode", "shouldEnterQaFixMode"]
    - path: "src/runtime/qa/report.ts"
      provides: "Evidence and severity schema for /qa results"
      exports: ["QaFinding", "QaReport"]
    - path: "src/skills/qa/build-qa-skill.ts"
      provides: "Builder for the checked-in /qa skill artifact"
      exports: ["buildQaSkillArtifact", "buildQaReadme"]
    - path: ".github/skills/qa/SKILL.md"
      provides: "Project-local /qa skill artifact"
  key_links:
    - from: "tests/runtime/qa-flow.test.ts"
      to: "src/runtime/qa/flow.ts"
      via: "import"
      pattern: "import.*resolveQaMode"
    - from: "tests/runtime/qa-report.test.ts"
      to: "src/runtime/qa/report.ts"
      via: "import"
      pattern: "import.*QaReport"
    - from: "tests/skills/qa-artifact.test.ts"
      to: "src/skills/qa/build-qa-skill.ts"
      via: "import"
      pattern: "import.*buildQaSkillArtifact"
---

<objective>
Port `/qa` into a checked-in Copilot skill that preserves the source skill's evidence-heavy QA posture while adopting the locked Phase 5 defaults: guided flow, findings first, and explicit confirmation before fixes.

Purpose: establish the first browser-testing skill on top of the Phase 4 browser runtime and make its contract verifiable in code instead of leaving it as prose only.

Output: `/qa` runtime helpers, builder, checked-in artifact, README, and focused runtime/artifact/integration tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-browser-skills-qa-office-hours/05-CONTEXT.md
@.planning/phases/05-browser-skills-qa-office-hours/05-RESEARCH.md
@.planning/phases/04-browser-abstraction/04-VERIFICATION.md
@src/runtime/browser/adapter.ts
@src/runtime/browser/capabilities.ts
@src/runtime/browser/fallbacks.ts
@src/skills/review/build-review-skill.ts
@.github/skills/review/SKILL.md
@C:/Users/Thinkpad/.codex/skills/gstack-qa/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the `/qa` guided-flow and report contract</name>
  <files>src/runtime/qa/flow.ts, src/runtime/qa/report.ts, tests/runtime/qa-flow.test.ts, tests/runtime/qa-report.test.ts</files>
  <read_first>
    - .planning/phases/05-browser-skills-qa-office-hours/05-CONTEXT.md
    - .planning/phases/05-browser-skills-qa-office-hours/05-RESEARCH.md
    - src/runtime/browser/adapter.ts
    - src/runtime/browser/capabilities.ts
    - C:/Users/Thinkpad/.codex/skills/gstack-qa/SKILL.md
  </read_first>
  <acceptance_criteria>
    - src/runtime/qa/flow.ts exports `resolveQaMode` and `shouldEnterQaFixMode`
    - `resolveQaMode` defaults to a guided-flow mode, not a crawl mode
    - `shouldEnterQaFixMode` requires explicit confirmation before returning true
    - src/runtime/qa/report.ts exports `QaFinding` and `QaReport`
    - `QaFinding` includes severity, repro steps, and screenshot fields
    - `QaReport` includes console/network evidence fields and a health-summary field
    - tests/runtime/qa-flow.test.ts exits 0
    - tests/runtime/qa-report.test.ts exits 0
  </acceptance_criteria>
  <action>
    Create `src/runtime/qa/flow.ts` with a small runtime contract that encodes the locked `/qa` behavior:
    - the default mode resolves to a guided-flow execution path
    - report-first is the default interaction contract
    - fix mode is only available after explicit confirmation

    Create `src/runtime/qa/report.ts` with a typed schema for `/qa` findings and summaries. Include:
    - severity values `critical`, `high`, `medium`, `low`
    - repro steps as an ordered list of strings
    - screenshot evidence as explicit artifact paths or descriptors
    - optional console evidence and optional network evidence
    - a health-summary structure so later execution can carry forward the source skill's health-score/ship-readiness posture without inventing a new schema later
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/qa-flow.test.ts tests/runtime/qa-report.test.ts</automated>
  </verify>
  <done>The repository has a real `/qa` runtime contract rather than prose-only expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the checked-in `/qa` skill artifact</name>
  <files>src/skills/qa/build-qa-skill.ts, .github/skills/qa/SKILL.md, .github/skills/qa/README.md, tests/fixtures/qa-skill.md, tests/skills/qa-artifact.test.ts</files>
  <read_first>
    - src/skills/review/build-review-skill.ts
    - .github/skills/review/SKILL.md
    - .planning/phases/05-browser-skills-qa-office-hours/05-CONTEXT.md
    - src/runtime/qa/flow.ts
    - src/runtime/qa/report.ts
    - C:/Users/Thinkpad/.codex/skills/gstack-qa/SKILL.md
  </read_first>
  <acceptance_criteria>
    - src/skills/qa/build-qa-skill.ts exports `buildQaSkillArtifact` and `buildQaReadme`
    - .github/skills/qa/SKILL.md exists and matches the builder output exactly
    - .github/skills/qa/SKILL.md contains sections describing guided-flow default, findings-first output, explicit confirm-before-fix behavior, and evidence expectations
    - .github/skills/qa/SKILL.md does not contain raw `$B` commands
    - .github/skills/qa/SKILL.md references screenshots, repro steps, and console/network evidence
    - tests/skills/qa-artifact.test.ts exits 0
  </acceptance_criteria>
  <action>
    Create `tests/fixtures/qa-skill.md` as a hermetic source fixture representing the gstack `/qa` input the builder transforms.

    Create `src/skills/qa/build-qa-skill.ts` following the `/review` builder pattern:
    - generate a checked-in project-local `/qa` artifact
    - keep the builder as the source of truth for `.github/skills/qa/SKILL.md`
    - encode the locked `/qa` defaults directly in artifact guidance

    Create `.github/skills/qa/SKILL.md` and `.github/skills/qa/README.md`.
    The artifact text must explicitly say:
    - guided flow is the default path
    - findings are reported before edits
    - fixes require explicit confirmation
    - evidence should include screenshots, repro steps, and console/network findings when relevant

    Rewrite any source references so the checked-in artifact points at repo-local skill/runtime paths rather than user-home gstack paths.
  </action>
  <verify>
    <automated>npx vitest run tests/skills/qa-artifact.test.ts</automated>
  </verify>
  <done>The repository has a checked-in `/qa` skill artifact aligned with the Phase 5 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add `/qa` builder and runtime integration coverage</name>
  <files>tests/integration/qa-runtime.test.ts, tests/integration/qa-convert.test.ts</files>
  <read_first>
    - src/runtime/qa/flow.ts
    - src/runtime/qa/report.ts
    - src/skills/qa/build-qa-skill.ts
    - .github/skills/qa/SKILL.md
    - tests/fixtures/qa-skill.md
  </read_first>
  <acceptance_criteria>
    - tests/integration/qa-runtime.test.ts exists and exits 0
    - tests/integration/qa-convert.test.ts exists and exits 0
    - integration tests assert the guided-flow default
    - integration tests assert fix mode remains gated by explicit confirmation
    - integration tests assert the checked-in artifact is parseable and contains the `/qa` evidence contract
  </acceptance_criteria>
  <action>
    Add integration coverage that validates the runtime contract and builder output together.

    `tests/integration/qa-runtime.test.ts` must cover:
    - guided-flow default resolution
    - explicit confirmation as the gate into fix mode
    - severity/evidence schema usage at the runtime boundary

    `tests/integration/qa-convert.test.ts` must cover:
    - builder output is parseable
    - the artifact includes the required `/qa` sections and text
    - the artifact text carries the evidence contract, not only the README
  </action>
  <verify>
    <automated>npx vitest run tests/integration/qa-runtime.test.ts tests/integration/qa-convert.test.ts</automated>
  </verify>
  <done>The `/qa` port is validated as a coherent runtime-plus-artifact slice.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/runtime/qa-flow.test.ts tests/runtime/qa-report.test.ts tests/skills/qa-artifact.test.ts tests/integration/qa-runtime.test.ts tests/integration/qa-convert.test.ts`
2. `npx tsc --noEmit`
3. `.github/skills/qa/SKILL.md` reflects the locked Phase 5 `/qa` decisions without raw `$B` commands
</verification>

<success_criteria>
- `/qa` runtime contract exists
- Guided-flow, findings-first, and explicit confirm-before-fix behavior are codified
- `/qa` checked-in artifact exists and is builder-backed
- Runtime, artifact, and integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-browser-skills-qa-office-hours/05-01-SUMMARY.md`
</output>
