---
phase: 05-browser-skills-qa-office-hours
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - tests/fixtures/office-hours-page.html
  - tests/integration/qa-browser-skill.test.ts
  - tests/integration/office-hours-browser-skill.test.ts
  - tests/integration/browser-skills-fallbacks.test.ts
  - .planning/phases/05-browser-skills-qa-office-hours/05-UAT.md
  - .planning/phases/05-browser-skills-qa-office-hours/05-VERIFICATION.md
autonomous: true
requirements:
  - SKILL-02
  - SKILL-03
must_haves:
  truths:
    - "A deterministic guided-flow scenario proves `/qa` can gather browser-grounded evidence on top of the Phase 4 backend."
    - "A deterministic product-page scenario proves `/office-hours` can generate browser-grounded critique plus memo output."
    - "Both browser skills degrade gracefully when the Chrome DevTools backend lacks optional capabilities."
  artifacts:
    - path: "tests/integration/qa-browser-skill.test.ts"
      provides: "Guided-flow /qa verification"
    - path: "tests/integration/office-hours-browser-skill.test.ts"
      provides: "Product-page /office-hours verification"
    - path: "tests/integration/browser-skills-fallbacks.test.ts"
      provides: "Graceful degradation coverage"
    - path: ".planning/phases/05-browser-skills-qa-office-hours/05-UAT.md"
      provides: "Manual/live Phase 5 acceptance checklist"
    - path: ".planning/phases/05-browser-skills-qa-office-hours/05-VERIFICATION.md"
      provides: "Goal-backward Phase 5 verification"
  key_links:
    - from: "tests/integration/qa-browser-skill.test.ts"
      to: "src/runtime/browser/chrome-devtools-backend.ts"
      via: "integration"
      pattern: "ChromeDevToolsBackend"
    - from: "tests/integration/office-hours-browser-skill.test.ts"
      to: "src/runtime/browser/chrome-devtools-backend.ts"
      via: "integration"
      pattern: "ChromeDevToolsBackend"
---

<objective>
Finish Phase 5 by proving both browser-heavy skills work against deterministic fixtures, documenting graceful degradation, and preparing the live UAT and final verification artifacts.

Purpose: close the loop from the new skill artifacts back to the Phase 4 browser runtime so later phases can rely on `/qa` and `/office-hours` as shipped browser skills instead of unverified markdown artifacts.

Output: deterministic skill-level integration tests, fallback coverage, Phase 5 UAT, and final verification.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-browser-skills-qa-office-hours/05-CONTEXT.md
@.planning/phases/05-browser-skills-qa-office-hours/05-RESEARCH.md
@.planning/phases/05-browser-skills-qa-office-hours/05-VALIDATION.md
@.planning/phases/04-browser-abstraction/04-VERIFICATION.md
@src/runtime/browser/chrome-devtools-backend.ts
@src/runtime/browser/fallbacks.ts
@src/runtime/qa/flow.ts
@src/runtime/qa/report.ts
@src/runtime/office-hours/mode.ts
@src/runtime/office-hours/memo.ts
@tests/fixtures/browser-flow/page.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic skill-level browser verification</name>
  <files>tests/fixtures/office-hours-page.html, tests/integration/qa-browser-skill.test.ts, tests/integration/office-hours-browser-skill.test.ts</files>
  <read_first>
    - tests/fixtures/browser-flow/page.html
    - src/runtime/browser/chrome-devtools-backend.ts
    - src/runtime/qa/flow.ts
    - src/runtime/qa/report.ts
    - src/runtime/office-hours/mode.ts
    - src/runtime/office-hours/memo.ts
  </read_first>
  <acceptance_criteria>
    - tests/fixtures/office-hours-page.html exists with stable selectors and product-page-style content
    - tests/integration/qa-browser-skill.test.ts exists and exits 0
    - tests/integration/office-hours-browser-skill.test.ts exists and exits 0
    - the `/qa` integration test covers a guided flow with screenshots or snapshot-style evidence, repro steps, and severity-bearing findings
    - the `/office-hours` integration test covers critique-first memo generation from a product-page-style fixture
  </acceptance_criteria>
  <action>
    Reuse `tests/fixtures/browser-flow/page.html` for `/qa` and add a second deterministic fixture `tests/fixtures/office-hours-page.html` representing a product page or product marketing surface.

    Create `tests/integration/qa-browser-skill.test.ts` that proves the `/qa` contract can be exercised against the browser backend through a deterministic guided flow. The test should validate:
    - browser-grounded evidence collection
    - repro-step structure
    - severity-bearing findings

    Create `tests/integration/office-hours-browser-skill.test.ts` that proves the `/office-hours` contract can analyze a product-style page and produce:
    - critique-first observations
    - a recommended direction
    - durable memo-ready structure
  </action>
  <verify>
    <automated>npx vitest run tests/integration/qa-browser-skill.test.ts tests/integration/office-hours-browser-skill.test.ts</automated>
  </verify>
  <done>Both Phase 5 skills have deterministic browser-facing verification paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add graceful-degradation coverage for both skills</name>
  <files>tests/integration/browser-skills-fallbacks.test.ts</files>
  <read_first>
    - src/runtime/browser/fallbacks.ts
    - src/runtime/browser/capabilities.ts
    - src/runtime/qa/flow.ts
    - src/runtime/office-hours/memo.ts
    - .planning/phases/05-browser-skills-qa-office-hours/05-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - tests/integration/browser-skills-fallbacks.test.ts exists and exits 0
    - the test asserts `/qa` degrades explicitly when optional console/network or other browser capabilities are unavailable
    - the test asserts `/office-hours` degrades explicitly when optional browser capabilities are unavailable
    - no skill path reports silent success when a required browser action cannot be completed
  </acceptance_criteria>
  <action>
    Add `tests/integration/browser-skills-fallbacks.test.ts` that validates the roadmap's graceful-degradation requirement at the skill level.

    The integration test must cover:
    - at least one `/qa` case where optional browser evidence is unavailable and the contract records explicit fallback guidance
    - at least one `/office-hours` case where optional browser observation support is unavailable and the contract records explicit fallback guidance
    - one essential-action case that fails rather than pretending the skill completed successfully
  </action>
  <verify>
    <automated>npx vitest run tests/integration/browser-skills-fallbacks.test.ts</automated>
  </verify>
  <done>Graceful degradation is tested rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Prepare Phase 5 UAT and verification artifacts</name>
  <files>.planning/phases/05-browser-skills-qa-office-hours/05-UAT.md, .planning/phases/05-browser-skills-qa-office-hours/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/05-browser-skills-qa-office-hours/05-VALIDATION.md
    - .planning/phases/04-browser-abstraction/04-UAT.md
    - .planning/phases/04-browser-abstraction/04-VERIFICATION.md
    - tests/integration/qa-browser-skill.test.ts
    - tests/integration/office-hours-browser-skill.test.ts
    - tests/integration/browser-skills-fallbacks.test.ts
  </read_first>
  <acceptance_criteria>
    - .planning/phases/05-browser-skills-qa-office-hours/05-UAT.md exists
    - 05-UAT.md includes one live `/qa` run, one live `/office-hours` run, and one graceful-degradation check
    - .planning/phases/05-browser-skills-qa-office-hours/05-VERIFICATION.md exists
    - 05-VERIFICATION.md evaluates all four Phase 5 success criteria from ROADMAP.md
    - 05-VERIFICATION.md distinguishes automated evidence from live manual verification
  </acceptance_criteria>
  <action>
    Create `05-UAT.md` and `05-VERIFICATION.md` after the integration tests exist.

    The UAT checklist must include:
    - one live `/qa` run through a guided browser flow
    - one live `/office-hours` run against a product-page-style target
    - one graceful-degradation or capability-gap sanity check

    The verification report must cover:
    - `/qa` can navigate a web app, detect issues, and report with screenshots
    - `/office-hours` can analyze a product page and generate YC-style design feedback
    - both skills use the browser abstraction rather than raw `$B`
    - both skills degrade gracefully when optional browser features are unavailable
  </action>
  <verify>
    <automated>findstr /i "qa office-hours fallback" .planning\phases\05-browser-skills-qa-office-hours\05-UAT.md && findstr /i "status: goal requirement" .planning\phases\05-browser-skills-qa-office-hours\05-VERIFICATION.md</automated>
  </verify>
  <done>Phase 5 has live/manual acceptance coverage and a goal-backward verification artifact ready for sign-off.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/integration/qa-browser-skill.test.ts tests/integration/office-hours-browser-skill.test.ts tests/integration/browser-skills-fallbacks.test.ts`
2. `npm test && npx tsc --noEmit`
3. `05-UAT.md` and `05-VERIFICATION.md` exist with the required sections
</verification>

<success_criteria>
- Deterministic verification exists for both Phase 5 skills
- Graceful-degradation behavior is tested
- Phase 5 has live UAT coverage for `/qa` and `/office-hours`
- Final verification covers all roadmap success criteria
</success_criteria>

<output>
After completion, create `.planning/phases/05-browser-skills-qa-office-hours/05-03-SUMMARY.md`
</output>
