---
phase: 03-first-skill-review
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .github/skills/review/SKILL.md
  - .github/skills/review/README.md
  - src/skills/review/build-review-skill.ts
  - tests/skills/review-artifact.test.ts
  - tests/integration/review-convert.test.ts
autonomous: true
requirements:
  - SKILL-01
must_haves:
  truths:
    - "A real project-local `/review` skill artifact exists in a valid Copilot skill directory."
    - "The skill presents findings before any fix step."
    - "The skill encodes an explicit confirmation step before applying fixes."
  artifacts:
    - path: ".github/skills/review/SKILL.md"
      provides: "Project-local `/review` skill artifact"
      contains: "/review"
    - path: "tests/skills/review-artifact.test.ts"
      provides: "Artifact validation coverage"
    - path: "tests/integration/review-convert.test.ts"
      provides: "End-to-end review artifact generation coverage"
  key_links:
    - from: "tests/skills/review-artifact.test.ts"
      to: ".github/skills/review/SKILL.md"
      via: "reads artifact"
      pattern: "review"
---

<objective>
Create the actual project-local `/review` Copilot skill artifact and encode the report-first, confirm-before-fix behavior.

Purpose: move from infrastructure to a real skill users can invoke in this repository. This plan should produce the concrete `SKILL.md` output that later UAT and verification can test.

Output: `.github/skills/review/SKILL.md`, supporting build logic if needed, and tests proving the artifact is correct.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-first-skill-review/03-CONTEXT.md
@.planning/phases/03-first-skill-review/03-RESEARCH.md
@tests/fixtures/review-skill.md
@src/runtime/review/base-branch.ts
@src/runtime/review/review-scope.ts
@src/pipeline/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the `/review` skill artifact generator</name>
  <files>src/skills/review/build-review-skill.ts, tests/integration/review-convert.test.ts</files>
  <read_first>
    - src/pipeline/index.ts
    - tests/fixtures/review-skill.md
    - .planning/phases/03-first-skill-review/03-CONTEXT.md
    - .planning/phases/03-first-skill-review/03-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - src/skills/review/build-review-skill.ts exists and exports the review artifact build entry point
    - tests/integration/review-convert.test.ts exists and exits 0
    - The builder can produce content for `.github/skills/review/SKILL.md` from the review source fixture or equivalent source input
  </acceptance_criteria>
  <action>
    Create a dedicated Phase 3 builder module under `src/skills/review/` that generates the final `/review` skill artifact using the completed converter pipeline plus any review-specific frontmatter or packaging adjustments required by Plan 03-01. Add an integration test that proves the builder emits the final review skill content deterministically.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/review-convert.test.ts</automated>
  </verify>
  <done>The `/review` artifact can be generated reproducibly through code and is covered by integration tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the project-local `/review` skill artifact</name>
  <files>.github/skills/review/SKILL.md, .github/skills/review/README.md, tests/skills/review-artifact.test.ts</files>
  <read_first>
    - .planning/phases/03-first-skill-review/03-CONTEXT.md
    - tests/fixtures/review-skill.md
    - src/skills/review/build-review-skill.ts
    - .planning/phases/02-command-translation-layer/02-VERIFICATION.md
  </read_first>
  <acceptance_criteria>
    - .github/skills/review/SKILL.md exists
    - .github/skills/review/SKILL.md contains a findings-first review flow
    - .github/skills/review/SKILL.md contains an explicit confirmation gate before any fix step
    - tests/skills/review-artifact.test.ts exists and exits 0
    - .github/skills/review/README.md describes how the skill is intended to be invoked locally for Phase 3 verification
  </acceptance_criteria>
  <action>
    Create the project-local skill directory `.github/skills/review/` and write `SKILL.md` as the actual Phase 3 `/review` artifact. The artifact must:
    - default to branch-diff review against the detected base branch
    - keep uncommitted local changes opt-in
    - present findings before any fix action
    - ask for explicit confirmation before applying fixes in the same session

    Add a small README in the same directory describing the artifact’s purpose and local verification usage. Add `tests/skills/review-artifact.test.ts` that checks for the required instructions and frontmatter.
  </action>
  <verify>
    <automated>npx vitest run tests/skills/review-artifact.test.ts</automated>
  </verify>
  <done>The repository contains a real, test-validated `/review` skill artifact under a valid Copilot skill location.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Encode report-first and confirm-to-fix behavior explicitly</name>
  <files>.github/skills/review/SKILL.md, tests/skills/review-artifact.test.ts, tests/integration/review-convert.test.ts</files>
  <read_first>
    - .github/skills/review/SKILL.md
    - .planning/phases/03-first-skill-review/03-CONTEXT.md
    - tests/skills/review-artifact.test.ts
  </read_first>
  <acceptance_criteria>
    - `.github/skills/review/SKILL.md` contains explicit review-first instructions
    - `.github/skills/review/SKILL.md` contains explicit user confirmation wording before fix mode
    - tests assert both the findings-first rule and the explicit confirmation rule
  </acceptance_criteria>
  <action>
    Tighten the final `/review` artifact and its tests so the Phase 3 behavioral decisions are explicit in the artifact, not merely implied:
    - findings must be shown before fixes
    - fixes can happen in the same session
    - fixes require explicit user confirmation
  </action>
  <verify>
    <automated>npx vitest run tests/skills/review-artifact.test.ts tests/integration/review-convert.test.ts</automated>
  </verify>
  <done>The skill artifact and tests make the report-first and confirm-to-fix flow unambiguous.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/skills/review-artifact.test.ts tests/integration/review-convert.test.ts`
2. `npx tsc --noEmit`
3. `.github/skills/review/SKILL.md` exists and matches the locked behavior decisions
</verification>

<success_criteria>
- A real project-local `/review` skill artifact exists
- The artifact uses the locked invocation and review-scope behavior
- The artifact is findings-first and fix-confirmed
- Artifact and generation tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-skill-review/03-02-SUMMARY.md`
</output>
