---
phase: 03-first-skill-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/copilot.ts
  - src/pipeline/frontmatter.ts
  - src/runtime/review/base-branch.ts
  - src/runtime/review/review-scope.ts
  - tests/schemas.test.ts
  - tests/runtime/review-base-branch.test.ts
  - tests/runtime/review-scope.test.ts
  - tests/skills/review-frontmatter.test.ts
autonomous: true
requirements:
  - SKILL-01
must_haves:
  truths:
    - "Phase 3 uses a Copilot skill contract aligned with current GitHub docs for required and optional frontmatter."
    - "Base branch autodetection follows the locked order: origin/main, origin/master, repo default branch."
    - "Review scope defaults to branch diff only; uncommitted changes are opt-in."
  artifacts:
    - path: "src/runtime/review/base-branch.ts"
      provides: "Base branch autodetection helper"
      exports: ["detectBaseBranch"]
    - path: "src/runtime/review/review-scope.ts"
      provides: "Review target resolution rules"
      exports: ["resolveReviewScope"]
    - path: "tests/runtime/review-base-branch.test.ts"
      provides: "Automated verification of branch autodetection order"
    - path: "tests/runtime/review-scope.test.ts"
      provides: "Automated verification of review scope rules"
  key_links:
    - from: "tests/runtime/review-base-branch.test.ts"
      to: "src/runtime/review/base-branch.ts"
      via: "import"
      pattern: "import.*detectBaseBranch"
    - from: "tests/runtime/review-scope.test.ts"
      to: "src/runtime/review/review-scope.ts"
      via: "import"
      pattern: "import.*resolveReviewScope"
---

<objective>
Reconcile the `/review` skill contract with current Copilot CLI docs and build the review-target foundation that later plans will use.

Purpose: make sure Phase 3 is built on a valid Copilot skill package shape and deterministic base-branch/review-scope behavior instead of relying on outdated Phase 1 assumptions.

Output: documented frontmatter compatibility, base branch detection helper, review scope helper, and tests that lock the Phase 3 decisions into code.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-first-skill-review/03-CONTEXT.md
@.planning/phases/03-first-skill-review/03-RESEARCH.md
@tests/fixtures/review-skill.md
@src/pipeline/frontmatter.ts
@src/schemas/copilot.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align Copilot skill contract with current docs</name>
  <files>src/schemas/copilot.ts, src/pipeline/frontmatter.ts, tests/schemas.test.ts, tests/skills/review-frontmatter.test.ts</files>
  <read_first>
    - src/schemas/copilot.ts
    - src/pipeline/frontmatter.ts
    - tests/schemas.test.ts
    - .planning/phases/03-first-skill-review/03-RESEARCH.md
    - tests/fixtures/review-skill.md
  </read_first>
  <acceptance_criteria>
    - src/schemas/copilot.ts validates the Phase 3 `/review` frontmatter using documented fields only, or clearly gates any compatibility field with a named option
    - tests/skills/review-frontmatter.test.ts exists and exits 0
    - tests/schemas.test.ts still exits 0 after schema updates
    - No test asserts undocumented frontmatter without an explicit compatibility rationale
  </acceptance_criteria>
  <action>
    Update the Copilot frontmatter schema and frontmatter transform so the `/review` port uses a contract grounded in the current GitHub Copilot CLI docs. Keep `name` and `description` mandatory. Support `allowed-tools`, `user-invocable`, and `disable-model-invocation` if needed for the `/review` artifact. If `argument-hint` is retained for compatibility, make that an explicit, isolated compatibility decision in code and tests rather than the default schema assumption.

    Add `tests/skills/review-frontmatter.test.ts` to validate the final `/review` frontmatter shape that Phase 3 will ship.
  </action>
  <verify>
    <automated>npx vitest run tests/schemas.test.ts tests/skills/review-frontmatter.test.ts</automated>
  </verify>
  <done>Skill contract matches the intended `/review` artifact and is covered by schema tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement base branch autodetection helper</name>
  <files>src/runtime/review/base-branch.ts, tests/runtime/review-base-branch.test.ts</files>
  <read_first>
    - .planning/phases/03-first-skill-review/03-CONTEXT.md
    - .planning/phases/03-first-skill-review/03-RESEARCH.md
    - src/cli/convert.ts
    - src/pipeline/index.ts
  </read_first>
  <acceptance_criteria>
    - src/runtime/review/base-branch.ts exports detectBaseBranch
    - detectBaseBranch checks origin/main before origin/master before repo default branch
    - tests/runtime/review-base-branch.test.ts exists and exits 0
    - The helper behavior is deterministic for repos with and without explicit default-branch metadata
  </acceptance_criteria>
  <action>
    Create `src/runtime/review/base-branch.ts` with a helper that resolves the review base branch in this exact order:
    1. `origin/main` if present
    2. `origin/master` if present
    3. the repository default branch as a fallback

    Keep the helper testable by isolating git-probing/parsing from the pure selection logic. Add `tests/runtime/review-base-branch.test.ts` to cover the locked precedence order and fallback behavior.
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/review-base-branch.test.ts</automated>
  </verify>
  <done>Base branch detection is implemented and locked by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement review scope resolution helper</name>
  <files>src/runtime/review/review-scope.ts, tests/runtime/review-scope.test.ts</files>
  <read_first>
    - .planning/phases/03-first-skill-review/03-CONTEXT.md
    - src/runtime/review/base-branch.ts
    - tests/fixtures/review-skill.md
  </read_first>
  <acceptance_criteria>
    - src/runtime/review/review-scope.ts exports resolveReviewScope
    - resolveReviewScope defaults to branch diff mode
    - resolveReviewScope requires explicit opt-in for uncommitted working-tree review
    - tests/runtime/review-scope.test.ts exists and exits 0
    - tests cover default branch-diff scope and opt-in local changes mode
  </acceptance_criteria>
  <action>
    Create `src/runtime/review/review-scope.ts` that encodes the Phase 3 scope rules:
    - default mode reviews branch diff against the detected base branch
    - local uncommitted changes are excluded unless explicitly requested
    - the helper returns a structured review-target object that later plans can consume

    Add `tests/runtime/review-scope.test.ts` as a dedicated scope test file rather than combining scope tests into the base-branch suite. Keep branch-precedence tests in `review-base-branch.test.ts` and scope-mode tests in `review-scope.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/review-base-branch.test.ts tests/runtime/review-scope.test.ts</automated>
  </verify>
  <done>The review target contract is implemented and matches the locked Phase 3 scope decisions.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/schemas.test.ts tests/skills/review-frontmatter.test.ts tests/runtime/review-base-branch.test.ts tests/runtime/review-scope.test.ts`
2. `npx tsc --noEmit`
3. Review helpers and frontmatter tests reflect the locked decisions from `03-CONTEXT.md`
</verification>

<success_criteria>
- Copilot skill contract for `/review` is reconciled with current docs
- Base branch autodetection follows the locked precedence order
- Review scope defaults to branch diff and keeps local changes opt-in
- All new helper and schema tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-skill-review/03-01-SUMMARY.md`
</output>
