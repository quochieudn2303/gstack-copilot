---
phase: 04-browser-abstraction
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - tests/fixtures/browser-flow/page.html
  - tests/integration/browser-flow.test.ts
  - tests/integration/browser-fallbacks.test.ts
  - .planning/phases/04-browser-abstraction/04-UAT.md
  - .planning/phases/04-browser-abstraction/04-VERIFICATION.md
autonomous: true
requirements:
  - BROWSE-01
  - BROWSE-02
  - BROWSE-03
must_haves:
  truths:
    - "A deterministic browser scenario proves the backend can execute a small multi-step flow."
    - "Unsupported browser actions are documented and verified with explicit fallback guidance."
    - "Phase 4 includes a live-browser UAT path for the Chrome DevTools backend."
  artifacts:
    - path: "tests/integration/browser-flow.test.ts"
      provides: "Deterministic flow verification"
    - path: "tests/integration/browser-fallbacks.test.ts"
      provides: "Fallback behavior verification"
    - path: ".planning/phases/04-browser-abstraction/04-UAT.md"
      provides: "Manual backend/UAT checklist"
    - path: ".planning/phases/04-browser-abstraction/04-VERIFICATION.md"
      provides: "Goal-backward phase verification"
  key_links:
    - from: "tests/integration/browser-flow.test.ts"
      to: "src/runtime/browser/chrome-devtools-backend.ts"
      via: "integration"
      pattern: "ChromeDevToolsBackend"
---

<objective>
Finish Phase 4 by proving the browser abstraction works in a deterministic flow, documenting fallback behavior, and preparing the live Chrome DevTools UAT path.

Purpose: close the loop between the adapter contract, the backend mapping, and real browser-oriented verification so later browser skills can build on a trusted base.

Output: deterministic fixture flow tests, fallback integration tests, Phase 4 UAT artifact, and a final verification report.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-browser-abstraction/04-CONTEXT.md
@.planning/phases/04-browser-abstraction/04-RESEARCH.md
@.planning/phases/04-browser-abstraction/04-VALIDATION.md
@src/runtime/browser/chrome-devtools-backend.ts
@src/runtime/browser/fallbacks.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic browser flow fixture and integration test</name>
  <files>tests/fixtures/browser-flow/page.html, tests/integration/browser-flow.test.ts</files>
  <read_first>
    - src/runtime/browser/chrome-devtools-backend.ts
    - src/runtime/browser/adapter.ts
    - .planning/phases/04-browser-abstraction/04-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - tests/fixtures/browser-flow/page.html exists with stable selectors and predictable content
    - tests/integration/browser-flow.test.ts exists and exits 0
    - The integration test covers a multi-step flow using at least navigate, click or hover, fill, and snapshot or screenshot semantics
  </acceptance_criteria>
  <action>
    Create a deterministic local browser-flow fixture under `tests/fixtures/browser-flow/` and add `tests/integration/browser-flow.test.ts` that proves the backend can support a small multi-step scenario. Use stable selectors and expected text so later phases can reuse the same fixture.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/browser-flow.test.ts</automated>
  </verify>
  <done>The repository has a reusable deterministic browser flow for backend verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unsupported-action integration coverage</name>
  <files>tests/integration/browser-fallbacks.test.ts</files>
  <read_first>
    - src/runtime/browser/fallbacks.ts
    - src/runtime/browser/chrome-devtools-backend.ts
    - .planning/phases/04-browser-abstraction/04-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - tests/integration/browser-fallbacks.test.ts exists and exits 0
    - The test asserts structured fallback guidance for non-essential unsupported actions
    - The test asserts fail-fast behavior when an essential unsupported action has no safe fallback
  </acceptance_criteria>
  <action>
    Add `tests/integration/browser-fallbacks.test.ts` that validates the locked unsupported-command policy:
    - structured fallback guidance first for non-essential unsupported actions
    - explicit failure only when the action is essential and no safe fallback exists
    - no silent partial success
  </action>
  <verify>
    <automated>npx vitest run tests/integration/browser-fallbacks.test.ts</automated>
  </verify>
  <done>The backend’s unsupported-command policy is proven at integration level.</done>
</task>

<task type="auto">
  <name>Task 3: Prepare Phase 4 UAT and verification artifacts</name>
  <files>.planning/phases/04-browser-abstraction/04-UAT.md, .planning/phases/04-browser-abstraction/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-browser-abstraction/04-VALIDATION.md
    - .planning/phases/03-first-skill-review/03-UAT.md
    - .planning/phases/03-first-skill-review/03-VERIFICATION.md
    - tests/integration/browser-flow.test.ts
    - tests/integration/browser-fallbacks.test.ts
  </read_first>
  <acceptance_criteria>
    - .planning/phases/04-browser-abstraction/04-UAT.md exists
    - 04-UAT.md includes a live-browser verification checklist for the Chrome DevTools backend
    - .planning/phases/04-browser-abstraction/04-VERIFICATION.md exists
    - 04-VERIFICATION.md evaluates all four Phase 4 success criteria from ROADMAP.md
  </acceptance_criteria>
  <action>
    Create the Phase 4 UAT and verification artifacts after the deterministic automated tests exist. The UAT checklist must include:
    - one live browser flow through the Chrome DevTools backend
    - one console/network diagnostic check
    - one unsupported-command fallback check

    The verification report must distinguish automated evidence from manual live-browser verification.
  </action>
  <verify>
    <automated>grep -iE 'browser|console|network|fallback' .planning/phases/04-browser-abstraction/04-UAT.md && grep -iE 'status:|goal achievement|human verification required' .planning/phases/04-browser-abstraction/04-VERIFICATION.md</automated>
  </verify>
  <done>Phase 4 has a manual/live UAT path and a final verification artifact ready for sign-off.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/integration/browser-flow.test.ts tests/integration/browser-fallbacks.test.ts`
2. `npm test && npx tsc --noEmit`
3. `04-UAT.md` and `04-VERIFICATION.md` exist with the required sections
</verification>

<success_criteria>
- Deterministic browser flow verification exists
- Unsupported-command fallback behavior is documented and tested
- Phase 4 has a live-browser UAT path
- Final verification artifact covers all Phase 4 success criteria
</success_criteria>

<output>
After completion, create `.planning/phases/04-browser-abstraction/04-03-SUMMARY.md`
</output>
