---
phase: 04-browser-abstraction
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/runtime/browser/chrome-devtools-backend.ts
  - src/runtime/browser/chrome-devtools-mapping.ts
  - tests/runtime/browser-chrome-devtools.test.ts
  - tests/integration/browser-command-mapping.test.ts
autonomous: true
requirements:
  - BROWSE-01
  - BROWSE-02
must_haves:
  truths:
    - "A chrome-devtools backend implements the core BrowserAdapter interface."
    - "Capability-gated methods map to the documented Chrome DevTools MCP surface where available."
    - "The backend can describe unsupported actions through the structured fallback model."
  artifacts:
    - path: "src/runtime/browser/chrome-devtools-backend.ts"
      provides: "Chrome DevTools backend implementation"
      exports: ["ChromeDevToolsBackend"]
    - path: "src/runtime/browser/chrome-devtools-mapping.ts"
      provides: "Method-to-tool mapping registry for the backend"
      exports: ["CHROME_DEVTOOLS_METHOD_MAP"]
    - path: "tests/runtime/browser-chrome-devtools.test.ts"
      provides: "Backend-level verification"
  key_links:
    - from: "tests/runtime/browser-chrome-devtools.test.ts"
      to: "src/runtime/browser/chrome-devtools-backend.ts"
      via: "import"
      pattern: "import.*ChromeDevToolsBackend"
---

<objective>
Implement the first concrete browser backend using the Chrome DevTools MCP surface and map the chosen adapter methods to the available browser tools.

Purpose: make Phase 4 deliver a usable backend for later skills, not just interfaces. This is where the adapter contract from Plan 04-01 becomes operational.

Output: Chrome DevTools backend, method/tool mapping registry, and backend tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-browser-abstraction/04-CONTEXT.md
@.planning/phases/04-browser-abstraction/04-RESEARCH.md
@src/runtime/browser/adapter.ts
@src/runtime/browser/capabilities.ts
@src/runtime/browser/fallbacks.ts
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Chrome DevTools method mapping registry</name>
  <files>src/runtime/browser/chrome-devtools-mapping.ts, tests/runtime/browser-chrome-devtools.test.ts</files>
  <read_first>
    - .planning/phases/04-browser-abstraction/04-RESEARCH.md
    - .planning/phases/04-browser-abstraction/04-CONTEXT.md
    - src/runtime/browser/adapter.ts
    - src/runtime/browser/capabilities.ts
  </read_first>
  <acceptance_criteria>
    - src/runtime/browser/chrome-devtools-mapping.ts exports CHROME_DEVTOOLS_METHOD_MAP
    - The mapping covers navigate, click, fill, screenshot, snapshot
    - The mapping also covers capability methods waitFor, evaluate, hover, console, and network
    - tests/runtime/browser-chrome-devtools.test.ts exists and exits 0
  </acceptance_criteria>
  <action>
    Create `src/runtime/browser/chrome-devtools-mapping.ts` as the registry that maps the browser abstraction methods to the Chrome DevTools MCP tool names. At minimum map:
    - navigate -> navigate_page
    - click -> click
    - fill -> fill or fill_form
    - screenshot -> take_screenshot
    - snapshot -> take_snapshot
    - waitFor -> wait_for
    - evaluate -> evaluate_script
    - hover -> hover
    - console -> list_console_messages
    - network -> list_network_requests
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/browser-chrome-devtools.test.ts</automated>
  </verify>
  <done>The repository has a concrete method/tool mapping for the Chrome DevTools backend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ChromeDevToolsBackend</name>
  <files>src/runtime/browser/chrome-devtools-backend.ts, tests/runtime/browser-chrome-devtools.test.ts</files>
  <read_first>
    - src/runtime/browser/adapter.ts
    - src/runtime/browser/capabilities.ts
    - src/runtime/browser/fallbacks.ts
    - src/runtime/browser/chrome-devtools-mapping.ts
    - .planning/phases/04-browser-abstraction/04-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - src/runtime/browser/chrome-devtools-backend.ts exports ChromeDevToolsBackend
    - ChromeDevToolsBackend implements the core adapter methods
    - Capability-gated methods return supported implementations or structured fallback guidance
    - tests/runtime/browser-chrome-devtools.test.ts exits 0
  </acceptance_criteria>
  <action>
    Create `src/runtime/browser/chrome-devtools-backend.ts` as the first concrete backend implementation. It should:
    - implement the required core adapter methods
    - surface optional capability methods when they are supported by the mapping registry
    - use the structured fallback model from Plan 04-01 when an action is unsupported or intentionally deferred

    Keep the backend implementation backend-specific but the public surface domain-oriented.
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/browser-chrome-devtools.test.ts</automated>
  </verify>
  <done>The Chrome DevTools backend exists and satisfies the Phase 4 adapter contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add command-mapping integration coverage</name>
  <files>tests/integration/browser-command-mapping.test.ts</files>
  <read_first>
    - src/runtime/browser/chrome-devtools-backend.ts
    - src/runtime/browser/chrome-devtools-mapping.ts
    - .planning/phases/04-browser-abstraction/04-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - tests/integration/browser-command-mapping.test.ts exists and exits 0
    - The integration test validates the core method mappings and at least two capability-gated mappings
    - Unsupported/deferred browser actions are asserted to return structured fallback data rather than silent success
  </acceptance_criteria>
  <action>
    Add `tests/integration/browser-command-mapping.test.ts` to validate the effective behavior of the backend plus mapping registry together. Cover:
    - core methods
    - at least two capability-gated methods
    - unsupported/deferred behavior
  </action>
  <verify>
    <automated>npx vitest run tests/integration/browser-command-mapping.test.ts</automated>
  </verify>
  <done>The backend and its tool mapping are validated together, not just in isolated unit tests.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/runtime/browser-chrome-devtools.test.ts tests/integration/browser-command-mapping.test.ts`
2. `npx tsc --noEmit`
3. ChromeDevToolsBackend implements the core surface and capability extensions through the mapping registry
</verification>

<success_criteria>
- Chrome DevTools backend exists
- Core methods map to the expected MCP tools
- Capability methods map correctly or return structured fallback guidance
- Backend and mapping integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-browser-abstraction/04-02-SUMMARY.md`
</output>
