---
phase: 04-browser-abstraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime/browser/adapter.ts
  - src/runtime/browser/capabilities.ts
  - src/runtime/browser/fallbacks.ts
  - tests/runtime/browser-adapter.test.ts
  - tests/runtime/browser-capabilities.test.ts
  - tests/runtime/browser-fallbacks.test.ts
autonomous: true
requirements:
  - BROWSE-01
  - BROWSE-03
must_haves:
  truths:
    - "The browser abstraction exposes a required core interface plus capability extensions."
    - "Unsupported browser actions return structured fallback guidance when they are non-essential."
    - "The adapter contract already leaves room for a future Playwright backend."
  artifacts:
    - path: "src/runtime/browser/adapter.ts"
      provides: "Core BrowserAdapter interface"
      exports: ["BrowserAdapter"]
    - path: "src/runtime/browser/capabilities.ts"
      provides: "Capability extension model"
      exports: ["BrowserCapabilities"]
    - path: "src/runtime/browser/fallbacks.ts"
      provides: "Structured unsupported-action and fallback guidance model"
      exports: ["BrowserFallback", "UnsupportedBrowserActionError"]
  key_links:
    - from: "tests/runtime/browser-adapter.test.ts"
      to: "src/runtime/browser/adapter.ts"
      via: "import"
      pattern: "import.*BrowserAdapter"
    - from: "tests/runtime/browser-capabilities.test.ts"
      to: "src/runtime/browser/capabilities.ts"
      via: "import"
      pattern: "import.*BrowserCapabilities"
---

<objective>
Define the BrowserAdapter contract, capability model, and structured fallback behavior that all Phase 4 and Phase 5 browser work will depend on.

Purpose: establish a stable browser abstraction boundary before any backend-specific mapping code is written. This is where the “core interface plus capability extensions” decision becomes concrete code.

Output: browser adapter types, capability model, unsupported/fallback model, and unit tests that lock the Phase 4 API.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-browser-abstraction/04-CONTEXT.md
@.planning/phases/04-browser-abstraction/04-RESEARCH.md
@.planning/research/PITFALLS.md
@src/runtime/review/base-branch.ts
@src/runtime/review/review-scope.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the core BrowserAdapter interface</name>
  <files>src/runtime/browser/adapter.ts, tests/runtime/browser-adapter.test.ts</files>
  <read_first>
    - .planning/phases/04-browser-abstraction/04-CONTEXT.md
    - .planning/phases/04-browser-abstraction/04-RESEARCH.md
    - .planning/ROADMAP.md
  </read_first>
  <acceptance_criteria>
    - src/runtime/browser/adapter.ts exports BrowserAdapter
    - BrowserAdapter includes exactly the required core methods: navigate, click, fill, screenshot, snapshot
    - tests/runtime/browser-adapter.test.ts exists and exits 0
    - The core interface is backend-agnostic and does not mention chrome-devtools-specific tool names
  </acceptance_criteria>
  <action>
    Create `src/runtime/browser/adapter.ts` with the required core BrowserAdapter interface. The core methods must be:
    - `navigate`
    - `click`
    - `fill`
    - `screenshot`
    - `snapshot`

    Keep the API domain-oriented and backend-agnostic. Do not encode `navigate_page`, `take_snapshot`, or other chrome-devtools-specific names directly into the interface.
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/browser-adapter.test.ts</automated>
  </verify>
  <done>The repository has a stable core browser abstraction interface with tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define capability extensions for broad QA surface</name>
  <files>src/runtime/browser/capabilities.ts, tests/runtime/browser-capabilities.test.ts</files>
  <read_first>
    - .planning/phases/04-browser-abstraction/04-CONTEXT.md
    - src/runtime/browser/adapter.ts
    - .planning/phases/04-browser-abstraction/04-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - src/runtime/browser/capabilities.ts exports BrowserCapabilities
    - The capability model includes waitFor, evaluate, hover, console, and network
    - The capability model does not include file upload, auth/session import, or responsive/device emulation
    - tests/runtime/browser-capabilities.test.ts exists and exits 0
  </acceptance_criteria>
  <action>
    Create `src/runtime/browser/capabilities.ts` to model capability extensions on top of the core adapter. Include capability-gated support for:
    - `waitFor`
    - `evaluate`
    - `hover`
    - `console`
    - `network`

    Explicitly leave out:
    - file upload
    - auth/session import
    - responsive/device emulation
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/browser-capabilities.test.ts</automated>
  </verify>
  <done>The capability extension surface matches the locked Phase 4 command-surface decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define structured fallback behavior</name>
  <files>src/runtime/browser/fallbacks.ts, tests/runtime/browser-fallbacks.test.ts</files>
  <read_first>
    - .planning/phases/04-browser-abstraction/04-CONTEXT.md
    - .planning/research/PITFALLS.md
    - src/runtime/browser/adapter.ts
    - src/runtime/browser/capabilities.ts
  </read_first>
  <acceptance_criteria>
    - src/runtime/browser/fallbacks.ts exports structured fallback types or errors
    - The fallback model includes reason, essentiality, and guidance fields
    - Non-essential unsupported actions can return structured fallback guidance
    - tests/runtime/browser-fallbacks.test.ts exists and exits 0
  </acceptance_criteria>
  <action>
    Create `src/runtime/browser/fallbacks.ts` with a structured unsupported-action model that supports the locked Phase 4 fallback policy:
    - return explicit fallback guidance when a non-essential action is unsupported
    - fail fast only when the action is essential and no safe fallback exists
    - do not silently continue with partial browser results

    Model this through typed fallback results or a typed unsupported-action error with:
    - reason
    - essentiality
    - fallback guidance
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/browser-fallbacks.test.ts</automated>
  </verify>
  <done>The browser abstraction now has a typed fallback model that later backends and skills can rely on.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/runtime/browser-adapter.test.ts tests/runtime/browser-capabilities.test.ts tests/runtime/browser-fallbacks.test.ts`
2. `npx tsc --noEmit`
3. The core interface and capability model reflect the locked Phase 4 decisions without backend-specific leakage
</verification>

<success_criteria>
- BrowserAdapter core interface exists
- Capability extension model exists and matches the chosen broad QA surface
- Structured fallback guidance is codified
- Unit tests pass for all three layers
</success_criteria>

<output>
After completion, create `.planning/phases/04-browser-abstraction/04-01-SUMMARY.md`
</output>
