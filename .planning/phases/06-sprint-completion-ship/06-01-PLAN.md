---
phase: 06-sprint-completion-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime/ship/preflight.ts
  - src/runtime/ship/pr-body.ts
  - src/skills/ship/build-ship-skill.ts
  - .github/skills/ship/SKILL.md
  - .github/skills/ship/README.md
  - tests/fixtures/ship-skill.md
  - tests/runtime/ship-preflight.test.ts
  - tests/runtime/ship-pr-body.test.ts
  - tests/skills/ship-artifact.test.ts
  - tests/integration/ship-runtime.test.ts
  - tests/integration/ship-convert.test.ts
autonomous: true
requirements:
  - SKILL-04
must_haves:
  truths:
    - "The checked-in `/ship` skill prepares and opens a PR by default rather than merging automatically."
    - "The `/ship` contract enforces strict preflight for dirty tree, missing remote, missing auth, and wrong branch."
    - "The `/ship` runtime can generate PR-facing summary content from the repository's planning artifacts."
  artifacts:
    - path: "src/runtime/ship/preflight.ts"
      provides: "Strict preflight contract for /ship"
      exports: ["evaluateShipPreflight", "ShipPreflightStatus"]
    - path: "src/runtime/ship/pr-body.ts"
      provides: "PR body generation contract for /ship"
      exports: ["createShipPullRequestBody"]
    - path: "src/skills/ship/build-ship-skill.ts"
      provides: "Builder for the checked-in /ship artifact"
      exports: ["buildShipSkillArtifact", "buildShipReadme"]
    - path: ".github/skills/ship/SKILL.md"
      provides: "Project-local /ship skill artifact"
  key_links:
    - from: "tests/runtime/ship-preflight.test.ts"
      to: "src/runtime/ship/preflight.ts"
      via: "import"
      pattern: "import.*evaluateShipPreflight"
    - from: "tests/runtime/ship-pr-body.test.ts"
      to: "src/runtime/ship/pr-body.ts"
      via: "import"
      pattern: "import.*createShipPullRequestBody"
    - from: "tests/skills/ship-artifact.test.ts"
      to: "src/skills/ship/build-ship-skill.ts"
      via: "import"
      pattern: "import.*buildShipSkillArtifact"
---

<objective>
Port `/ship` into a checked-in Copilot skill with a testable runtime contract for strict preflight and PR preparation.

Purpose: close the core skill loop with a shipping skill that reflects the locked Phase 6 behavior without trying to clone the full source gstack `/ship` workflow verbatim.

Output: `/ship` runtime helpers, builder, checked-in artifact, README, and focused runtime/artifact/integration tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-sprint-completion-ship/06-CONTEXT.md
@.planning/phases/06-sprint-completion-ship/06-RESEARCH.md
@.planning/phases/05-browser-skills-qa-office-hours/05-VERIFICATION.md
@src/skills/review/build-review-skill.ts
@src/skills/qa/build-qa-skill.ts
@src/skills/office-hours/build-office-hours-skill.ts
@.github/skills/review/SKILL.md
@.github/skills/qa/SKILL.md
@.github/skills/office-hours/SKILL.md
@C:/Users/Thinkpad/.codex/skills/gstack-ship/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict `/ship` preflight and PR-body contracts</name>
  <files>src/runtime/ship/preflight.ts, src/runtime/ship/pr-body.ts, tests/runtime/ship-preflight.test.ts, tests/runtime/ship-pr-body.test.ts</files>
  <read_first>
    - .planning/phases/06-sprint-completion-ship/06-CONTEXT.md
    - .planning/phases/06-sprint-completion-ship/06-RESEARCH.md
    - .planning/phases/05-browser-skills-qa-office-hours/05-VERIFICATION.md
    - C:/Users/Thinkpad/.codex/skills/gstack-ship/SKILL.md
  </read_first>
  <acceptance_criteria>
    - src/runtime/ship/preflight.ts exports `evaluateShipPreflight` and a `ShipPreflightStatus` type
    - `evaluateShipPreflight` can represent at least clean, dirty-tree, missing-remote, missing-auth, and wrong-branch outcomes
    - src/runtime/ship/pr-body.ts exports `createShipPullRequestBody`
    - `createShipPullRequestBody` includes summary, verification, and requirements coverage sections
    - tests/runtime/ship-preflight.test.ts exits 0
    - tests/runtime/ship-pr-body.test.ts exits 0
  </acceptance_criteria>
  <action>
    Create `src/runtime/ship/preflight.ts` as the strict preflight contract for `/ship`. Model explicit status results for:
    - clean and ready
    - dirty working tree
    - missing remote
    - missing GitHub auth
    - wrong branch for shipping

    Create `src/runtime/ship/pr-body.ts` to build a PR-facing summary from repository artifacts. The generated structure must include:
    - summary of shipped work
    - verification or test coverage section
    - requirements addressed section

    Keep the runtime contract narrow and testable. Do not attempt to implement the full source skill's versioning, TODO, or doc sync logic here.
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/ship-preflight.test.ts tests/runtime/ship-pr-body.test.ts</automated>
  </verify>
  <done>The repository has a testable `/ship` runtime contract for preflight and PR preparation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the checked-in `/ship` skill artifact</name>
  <files>src/skills/ship/build-ship-skill.ts, .github/skills/ship/SKILL.md, .github/skills/ship/README.md, tests/fixtures/ship-skill.md, tests/skills/ship-artifact.test.ts</files>
  <read_first>
    - src/skills/review/build-review-skill.ts
    - src/skills/qa/build-qa-skill.ts
    - .github/skills/qa/SKILL.md
    - .planning/phases/06-sprint-completion-ship/06-CONTEXT.md
    - src/runtime/ship/preflight.ts
    - src/runtime/ship/pr-body.ts
    - C:/Users/Thinkpad/.codex/skills/gstack-ship/SKILL.md
  </read_first>
  <acceptance_criteria>
    - src/skills/ship/build-ship-skill.ts exports `buildShipSkillArtifact` and `buildShipReadme`
    - .github/skills/ship/SKILL.md exists and matches the builder output exactly
    - .github/skills/ship/SKILL.md explicitly says the default behavior is prepare-and-open-PR, not auto-merge
    - .github/skills/ship/SKILL.md explicitly documents strict preflight behavior
    - .github/skills/ship/SKILL.md references `/qa` and `/review` as prior-signal inputs when relevant
    - tests/skills/ship-artifact.test.ts exits 0
  </acceptance_criteria>
  <action>
    Create `tests/fixtures/ship-skill.md` as a hermetic source fixture representing the source gstack `/ship` input.

    Create `src/skills/ship/build-ship-skill.ts` following the existing builder-backed artifact pattern:
    - generate a checked-in project-local `/ship` artifact
    - keep the builder as the source of truth for `.github/skills/ship/SKILL.md`
    - encode the locked Phase 6 defaults directly in artifact guidance

    Create `.github/skills/ship/SKILL.md` and `.github/skills/ship/README.md`.
    The artifact text must explicitly say:
    - prepare and open PR by default
    - do not merge automatically
    - strict preflight is required
    - previous verification signals from `/review`, `/qa`, and existing planning artifacts should be used when available
  </action>
  <verify>
    <automated>npx vitest run tests/skills/ship-artifact.test.ts</automated>
  </verify>
  <done>The repository has a checked-in `/ship` artifact aligned with the Phase 6 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add `/ship` runtime and builder integration coverage</name>
  <files>tests/integration/ship-runtime.test.ts, tests/integration/ship-convert.test.ts</files>
  <read_first>
    - src/runtime/ship/preflight.ts
    - src/runtime/ship/pr-body.ts
    - src/skills/ship/build-ship-skill.ts
    - .github/skills/ship/SKILL.md
    - tests/fixtures/ship-skill.md
  </read_first>
  <acceptance_criteria>
    - tests/integration/ship-runtime.test.ts exists and exits 0
    - tests/integration/ship-convert.test.ts exists and exits 0
    - integration tests assert strict preflight outcomes
    - integration tests assert the checked-in artifact is parseable and preserves the prepare-and-open-PR default
    - integration tests assert the `/ship` artifact references prior-skill signals without requiring auto-merge behavior
  </acceptance_criteria>
  <action>
    Add integration coverage that validates the `/ship` runtime and builder output together.

    `tests/integration/ship-runtime.test.ts` must cover:
    - strict preflight outcomes
    - PR-body generation shape

    `tests/integration/ship-convert.test.ts` must cover:
    - builder output is parseable
    - checked-in artifact text preserves the Phase 6 operating contract
    - checked-in artifact references the prior sprint-loop skills where relevant
  </action>
  <verify>
    <automated>npx vitest run tests/integration/ship-runtime.test.ts tests/integration/ship-convert.test.ts</automated>
  </verify>
  <done>The `/ship` port is validated as a coherent runtime-plus-artifact slice.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/runtime/ship-preflight.test.ts tests/runtime/ship-pr-body.test.ts tests/skills/ship-artifact.test.ts tests/integration/ship-runtime.test.ts tests/integration/ship-convert.test.ts`
2. `npx tsc --noEmit`
3. `.github/skills/ship/SKILL.md` reflects the locked Phase 6 `/ship` behavior
</verification>

<success_criteria>
- `/ship` runtime contract exists
- Strict preflight and PR-prep behavior are codified
- `/ship` checked-in artifact exists and is builder-backed
- Runtime, artifact, and integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-sprint-completion-ship/06-01-SUMMARY.md`
</output>
