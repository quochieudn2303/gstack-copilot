---
phase: 06-sprint-completion-ship
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/setup/install.ts
  - src/cli/setup.ts
  - src/cli/index.ts
  - setup.ps1
  - tests/runtime/setup-install.test.ts
  - tests/cli/setup-command.test.ts
  - tests/integration/setup-install.test.ts
autonomous: true
requirements:
  - SETUP-01
must_haves:
  truths:
    - "The repository ships both `setup.ps1` and `npx gstack-copilot setup`."
    - "Both setup entrypoints use the same install behavior rather than diverging."
    - "The setup path can verify or install the checked-in skill artifacts into the expected repo-local layout."
  artifacts:
    - path: "src/setup/install.ts"
      provides: "Shared install/setup implementation"
      exports: ["runSetupInstall"]
    - path: "src/cli/setup.ts"
      provides: "CLI setup command"
      exports: ["setupCommand"]
    - path: "setup.ps1"
      provides: "Windows-first setup entrypoint"
    - path: "src/cli/index.ts"
      provides: "CLI registration for setup"
  key_links:
    - from: "tests/runtime/setup-install.test.ts"
      to: "src/setup/install.ts"
      via: "import"
      pattern: "import.*runSetupInstall"
    - from: "tests/cli/setup-command.test.ts"
      to: "src/cli/setup.ts"
      via: "import"
      pattern: "import.*setupCommand"
---

<objective>
Create the shared setup/install surface for the Copilot CLI port and expose it through both the Windows-first script and the CLI command.

Purpose: satisfy the Phase 6 setup requirement without letting `setup.ps1` and `npx gstack-copilot setup` drift into separate implementations.

Output: shared setup implementation, CLI command, PowerShell entrypoint, and setup verification tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-sprint-completion-ship/06-CONTEXT.md
@.planning/phases/06-sprint-completion-ship/06-RESEARCH.md
@src/cli/index.ts
@src/cli/convert.ts
@src/pipeline/index.ts
@.github/skills/review/SKILL.md
@.github/skills/qa/SKILL.md
@.github/skills/office-hours/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared setup implementation</name>
  <files>src/setup/install.ts, tests/runtime/setup-install.test.ts</files>
  <read_first>
    - .planning/phases/06-sprint-completion-ship/06-CONTEXT.md
    - .planning/phases/06-sprint-completion-ship/06-RESEARCH.md
    - src/pipeline/index.ts
    - .github/skills/review/SKILL.md
    - .github/skills/qa/SKILL.md
    - .github/skills/office-hours/SKILL.md
  </read_first>
  <acceptance_criteria>
    - src/setup/install.ts exports `runSetupInstall`
    - the shared setup implementation can enumerate the checked-in skill artifacts
    - the shared setup implementation has a deterministic output model suitable for both CLI and script entrypoints
    - tests/runtime/setup-install.test.ts exits 0
  </acceptance_criteria>
  <action>
    Create `src/setup/install.ts` as the single source of truth for setup behavior.

    The implementation should:
    - discover the checked-in skill directories under `.github/skills/`
    - model a deterministic install or verify result
    - be callable from both the CLI command and the PowerShell script without duplicating logic

    Keep the initial Phase 6 scope focused on the current repository's local install/verify behavior rather than speculative global installation flows.
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/setup-install.test.ts</automated>
  </verify>
  <done>The repository has one shared setup implementation to drive both entrypoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose setup through the CLI and PowerShell entrypoints</name>
  <files>src/cli/setup.ts, src/cli/index.ts, setup.ps1, tests/cli/setup-command.test.ts, tests/integration/setup-install.test.ts</files>
  <read_first>
    - src/setup/install.ts
    - src/cli/index.ts
    - src/cli/convert.ts
    - .planning/phases/06-sprint-completion-ship/06-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - src/cli/setup.ts exports `setupCommand`
    - src/cli/index.ts registers the `setup` command
    - `setup.ps1` exists and calls into the shared setup behavior or the CLI entrypoint consistently
    - tests/cli/setup-command.test.ts exits 0
    - tests/integration/setup-install.test.ts exits 0
  </acceptance_criteria>
  <action>
    Create `src/cli/setup.ts` and register it in `src/cli/index.ts`.

    Create `setup.ps1` as the Windows-first entrypoint. It must align to the same shared setup behavior used by `npx gstack-copilot setup`. That can be achieved by invoking the built CLI or a shared implementation path, but the resulting behavior must stay aligned.

    Add tests that verify:
    - the CLI command is registered
    - the shared setup implementation is what both entrypoints use
    - the install/verify output stays consistent
  </action>
  <verify>
    <automated>npx vitest run tests/cli/setup-command.test.ts tests/integration/setup-install.test.ts</automated>
  </verify>
  <done>Both setup entrypoints exist and remain behaviorally aligned.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/runtime/setup-install.test.ts tests/cli/setup-command.test.ts tests/integration/setup-install.test.ts`
2. `npx tsc --noEmit`
3. `setup.ps1` and `npx gstack-copilot setup` both exist and align to the shared setup path
</verification>

<success_criteria>
- Shared setup implementation exists
- CLI setup command exists
- PowerShell setup script exists
- Setup verification tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-sprint-completion-ship/06-02-SUMMARY.md`
</output>
